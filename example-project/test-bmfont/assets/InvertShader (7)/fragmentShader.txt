uniform sampler2D map;
varying vec2 vUv;

void main() {
  vec2 uv = vUv;
  uv.x += sin(uv.y * 100.0) * 0.01;
  vec4 texCol = texture2D(map, uv);
  gl_FragColor.r = 1.0 - texCol.b;
  gl_FragColor.gb = texCol.rg;
  gl_FragColor.a = texCol.a;
}
